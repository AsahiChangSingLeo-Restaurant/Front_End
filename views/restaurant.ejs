<%-include('partials/header3.ejs') %>
<!DOCTYPE html>
<html>
  <head>
    <title>Order Form</title>
   
  </head>
  <body>
   
    <ul class="list-group">
      <% if(userCartList) {%>
      <h1 class="my-0">Name: <%= userCartList[0].name%></h1>
      <% } %>
  <%if(userCartItem){%>
       <%userCartItem.forEach(function(cart) {%>
      <li class="list-group-item d-flex justify-content-between lh-condensed">
        
        <div>
         
          <h2> Order Name: <%= cart.name%></h2>
          <small class="text-muted"> Quantity : <%=cart.quantity%></small>
        </div>
        <span class="text-muted"><%=cart.SubTotalPrice%> THB</span>
      </li>
<%});%>

    </ul>

    <% } %>
   <span>Total Price: </span>
   <strong><%=totalPrice%></strong>
   <span>THB</span>
<div class="buttonc">
      <div class="button-container">
        <button class="btn btn-danger delete-button" type="button">Delete</button>
      </div>
      <div class="button-container">
        <button class="btn btn-primary submit-button" type="submit">Submit</button>
      </div>
    </div>
    </form>
  </body>
</html>

<%- include('partials/footer') %> 
